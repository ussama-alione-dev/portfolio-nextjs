"use client";

const Footer = () => {
    return (
        <footer className="w-full text-center  py-4 border-t border-border">
            <p>Made with love by ali1 — {new Date().getFullYear()}</p>
        </footer>
    );
};

export default Footer;
